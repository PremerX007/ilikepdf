name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  rust:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.98.1
          components: clippy,rustfmt
      - run: cargo fmt --all -- --check
      - run: cargo clippy --workspace --all-targets -- -D warnings
      - run: cargo test --workspace --all-targets

  flutter:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: apps/desktop
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.98.1
          components: clippy,rustfmt
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.3
          cache: true
      - run: flutter pub get
      - run: dart format --output=none --set-exit-if-changed lib test integration_test hook
      - run: flutter analyze
      - run: flutter test
      - run: flutter build windows --release
      - name: Verify bundled PDFium runtime
        shell: pwsh
        run: |
          if (-not (Test-Path "build/windows/x64/runner/Release/pdfium.dll")) {
            throw "Release bundle is missing pdfium.dll"
          }
      - run: flutter test integration_test/app_info_test.dart -d windows
      - run: flutter test integration_test/pdf_preview_test.dart -d windows
